<h2> Listing Category: <%= @categories.length %> </h2>
<ol>
<% @categories.each do |category| %>
<li> <%= link_to category.name ,category_path(category.id)%> 
(<%= Article.where('category_id = ?',category.id).length %>)
<%#= link_to "show",category_path(category.id) %>
</li>
<% end %>
</ol>


<%= link_to "Add New Category",new_category_path,class: "btn btn-primary"%>